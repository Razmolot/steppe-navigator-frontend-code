# Production frontend build configuration
# Vite embeds this value into the generated JS bundle at build time.
# Keep it pointed at the public production origin so browser API calls go to
# https://steppe-navigator.kz/api/... rather than the local fallback.
VITE_API_URL=https://steppe-navigator.kz
